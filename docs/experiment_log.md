# 实验日志（Experiment Log）

> **铁律**：跑完实验立刻记录。否则一个月后你必然忘记当时的配置和结论。
> 每条记录包含：日期、目的、配置、命令、结果、结论。

---

## 模板（复制下面这段用）

```
### YYYY-MM-DD · 实验标题

**目的**：这次实验要回答什么问题？（例如：验证策略 A 是否提升 F5 的收敛精度）

**配置**：
- 算法：
- 测试集 / 维度 / MaxFES / 独立运行次数：
- 关键参数：
- 随机种子：

**命令**：
```bash
python experiments/run_main.py --runs 30 --max-fes 300000
```

**结果**：
- 结果文件：results/tables/xxx.csv
- 关键数字：

**结论**：
- 得到了什么？下一步做什么？

**异常**：
- 有没有跑崩、数值溢出、结果反常？
```

---

## 记录

### 2026-XX-XX · 仓库初始化基线实验

**目的**：验证实验流程是否跑通，建立 PSO / DE / GWO 的基线数据。

**配置**：dim=30, max_fes=20000, runs=5, pop_size=50，6 个经典函数（平移+旋转）。

**命令**：
```bash
python experiments/run_main.py
```

**结果**：见 `results/tables/summary.csv`。

**结论**：流程跑通。正式实验需把 runs 改为 30、max_fes 改为 300000，并接入 CEC 基准集。

**异常**：无。
